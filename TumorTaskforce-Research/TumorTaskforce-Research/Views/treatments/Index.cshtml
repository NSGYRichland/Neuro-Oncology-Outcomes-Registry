@model IEnumerable<TumorTaskforce_Research.treatment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Treatment_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Treatment_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GlobalTreatment_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GlobalTreatment_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GlobalTreatment_ID })
        </td>
    </tr>
}

</table>
