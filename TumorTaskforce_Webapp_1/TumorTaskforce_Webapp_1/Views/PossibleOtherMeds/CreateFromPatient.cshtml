@model TumorTaskforce_Webapp_1.PossibleOtherMed

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>New Other Medication</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.TempPatientID)
        <div class="form-group">
            @Html.LabelFor(model => model.Name, "Medication Name",htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>
        </div>
       
        

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to Add Other Med", "Create", "OtherMedsPivots", new { patientID = Int32.Parse(Html.ValueFor(model=>model.TempPatientID).ToString()) }, null)
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
